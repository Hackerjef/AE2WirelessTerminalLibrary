modLoader="javafml"
loaderVersion="[34,)"
issueTrackerURL="https://github.com/Hackerjef/AE2WirelessTerminalLibrary/issues/" #optional
license="MIT"
[[mods]]
modId="ae2wctlib"
version="${file.jarVersion}"
displayName="AE2 Wireless Crafting Terminal" #mandatory
authors="mari_023,Tfarecnim, pegasi_51b, nadie" #optional
description='''
A Library which provides basic functions to create AE2 Wireless Terminals with infinite range capabilities
This fork leaves only Wireless Crafting Terminal and support for AEInfinityBooster
'''
[[dependencies.ae2wctlib]] #optional
modId="appliedenergistics2" #mandatory
# Does this dependency have to exist - if not, ordering below must be specified
mandatory=true #mandatory
# The version range of the dependency
versionRange="[8.4.7,)" #mandatory
# An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
ordering="NONE"
# Side this dependency is applied on - BOTH, CLIENT or SERVER
side="BOTH"
[[dependencies.ae2wctlib]] #optional
modId="aeinfinitybooster" #mandatory
# Does this dependency have to exist - if not, ordering below must be specified
mandatory=true #mandatory
# The version range of the dependency
versionRange="[1.0.0,)" #mandatory
# An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
ordering="NONE"
# Side this dependency is applied on - BOTH, CLIENT or SERVER
side="BOTH"